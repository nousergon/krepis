{
  "$defs": {
    "ResolveCachePricing": {
      "additionalProperties": true,
      "description": "Price card for the resolved entry, in USD per 1M tokens.",
      "properties": {
        "cost_per_1m_input": {
          "anyOf": [
            {
              "type": "number"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "title": "Cost Per 1M Input"
        },
        "cost_per_1m_output": {
          "anyOf": [
            {
              "type": "number"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "title": "Cost Per 1M Output"
        },
        "cost_per_1m_cache_read": {
          "anyOf": [
            {
              "type": "number"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "title": "Cost Per 1M Cache Read"
        },
        "cost_per_1m_cache_write": {
          "anyOf": [
            {
              "type": "number"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "title": "Cost Per 1M Cache Write"
        }
      },
      "title": "ResolveCachePricing",
      "type": "object"
    },
    "ResolveCapabilities": {
      "additionalProperties": true,
      "description": "Per-model feature flags as declared in the registry.",
      "properties": {
        "web_search": {
          "anyOf": [
            {
              "type": "boolean"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "title": "Web Search"
        },
        "tool_choice": {
          "anyOf": [
            {
              "type": "boolean"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "title": "Tool Choice"
        },
        "prompt_caching": {
          "anyOf": [
            {
              "type": "boolean"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "title": "Prompt Caching"
        },
        "automatic_prefix_caching": {
          "anyOf": [
            {
              "type": "boolean"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "title": "Automatic Prefix Caching"
        },
        "batches": {
          "anyOf": [
            {
              "type": "boolean"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "title": "Batches"
        },
        "streaming": {
          "anyOf": [
            {
              "type": "boolean"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "title": "Streaming"
        }
      },
      "title": "ResolveCapabilities",
      "type": "object"
    },
    "SkippedEntry": {
      "additionalProperties": true,
      "description": "One registry entry the resolver did not use, and why.",
      "properties": {
        "registry_id": {
          "anyOf": [
            {
              "type": "string"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "title": "Registry Id"
        },
        "provider": {
          "anyOf": [
            {
              "type": "string"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "title": "Provider"
        },
        "reason": {
          "anyOf": [
            {
              "type": "string"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "title": "Reason"
        }
      },
      "title": "SkippedEntry",
      "type": "object"
    }
  },
  "$id": "https://nousergon.dev/schemas/krepis-router-resolve-v2.json",
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "additionalProperties": true,
  "description": "The routing decision returned by ``krepis.router.resolve_group_structured()``\nand printed by ``python3 -m krepis.router resolve <group> --json``.\n\nThis is a CROSS-REPO CONTRACT: consumers live in alpha-engine-config (groom\ndriver, groom_run.sh, disposition audit, reviewed-merge sweep) and\nclaude-code-config (the clauder wrapper). Evolve it ADDITIVELY -- emit a new\nfield alongside the old for one release, migrate consumers, then remove. A\nsame-commit rename has broken every consumer twice\n(alpha-engine-config-I4453, and the resolve-group->resolve CLI rename before\nit).",
  "properties": {
    "schema_version": {
      "description": "Contract version. Consumers MUST branch on this rather than probing for fields.",
      "minimum": 2,
      "title": "Schema Version",
      "type": "integer"
    },
    "model": {
      "description": "Model identifier to send on the wire. On the litellm_proxy route from resolve_group_structured() this is a MODEL GROUP -- the bare group name, or '{group}-cap-{sorted capabilities}' when the caller declared `requires` -- because a fallback chain is declared on a group and addressing a concrete deployment silently opts out of it (alpha-engine-config-I10399, reversing config-I6727). From resolve_model_structured() it is the registry entry id, which is a deliberate pin with no chain. Otherwise the concrete registry model string.",
      "title": "Model",
      "type": "string"
    },
    "display_name": {
      "description": "Human-readable label, e.g. 'deepseek-v4-pro (high)'. Display only -- never parse.",
      "title": "Display Name",
      "type": "string"
    },
    "provider": {
      "description": "Upstream provider, or 'litellm' when the central router is serving.",
      "title": "Provider",
      "type": "string"
    },
    "route": {
      "description": "Which transport serves this group.",
      "enum": [
        "litellm_proxy",
        "egress_proxy",
        "openrouter",
        "direct"
      ],
      "title": "Route",
      "type": "string"
    },
    "api_base_url": {
      "description": "Base URL the client must target. Empty string means the provider SDK default (only valid for route=direct). Consumers MUST fail closed rather than treating a missing value as empty -- an empty base URL silently targets api.anthropic.com.",
      "title": "Api Base Url",
      "type": "string"
    },
    "anthropic_base_url": {
      "anyOf": [
        {
          "type": "string"
        },
        {
          "type": "null"
        }
      ],
      "default": null,
      "deprecated": true,
      "description": "DEPRECATED alias for api_base_url, emitted through schema_version 2 only so unmigrated consumers keep working. Removed at schema_version 3. Do not add new reads.",
      "title": "Anthropic Base Url"
    },
    "deployment_id": {
      "description": "The model identifier to send on the wire (bare name for egress_proxy, full slug for openrouter; on litellm_proxy a MODEL GROUP for a group resolution -- see `model` -- or the registry entry id for a single-model resolution). Kept as an alias of `model` for consumers that read this key.",
      "title": "Deployment Id",
      "type": "string"
    },
    "wire_addressing": {
      "anyOf": [
        {
          "enum": [
            "group",
            "capability_group",
            "deployment"
          ],
          "type": "string"
        },
        {
          "type": "null"
        }
      ],
      "default": null,
      "description": "What KIND of name `model`/`deployment_id` is. 'group' and 'capability_group' are names LiteLLM applies a fallback chain to; 'deployment' addresses one concrete entry and has no chain. A consumer or auditor can tell the two apart without pattern-matching the string (alpha-engine-config-I10399). Absent from producers older than that fix.",
      "title": "Wire Addressing"
    },
    "auth_token_type": {
      "description": "Which credential the caller must present. 'placeholder' means the egress proxy injects the real key.",
      "enum": [
        "placeholder",
        "openrouter_key",
        "litellm_master_key",
        "direct_api_key"
      ],
      "title": "Auth Token Type",
      "type": "string"
    },
    "group": {
      "description": "The model group that was resolved (low|med|high|ultra). EMPTY STRING on a single-model resolution whose id is not a member of any model_groups chain -- several entries carry a vestigial top-level `group` field the registry itself does not read, and reporting it would claim a membership the registry denies.",
      "title": "Group",
      "type": "string"
    },
    "registry_id": {
      "description": "Registry entry id that served this resolution, or 'litellm:group:<group>' for a group resolution on the router edge (which names no entry -- see primary_registry_id). A single-model resolution always names the real entry.",
      "title": "Registry Id",
      "type": "string"
    },
    "primary_model": {
      "anyOf": [
        {
          "type": "string"
        },
        {
          "type": "null"
        }
      ],
      "default": null,
      "description": "The group's declared primary model. Present on the litellm_proxy route; the model actually served may differ if the proxy fell back. On a single-model resolution there is no chain, so this is the pinned entry's own upstream model string and cannot differ from what served.",
      "title": "Primary Model"
    },
    "primary_registry_id": {
      "anyOf": [
        {
          "type": "string"
        },
        {
          "type": "null"
        }
      ],
      "default": null,
      "description": "Registry id of the group's declared primary; on a single-model resolution, the pinned entry's own id.",
      "title": "Primary Registry Id"
    },
    "capabilities": {
      "$ref": "#/$defs/ResolveCapabilities"
    },
    "params": {
      "additionalProperties": true,
      "description": "Model params from the registry (max_tokens, reasoning, structured_outputs).",
      "title": "Params",
      "type": "object"
    },
    "cache_pricing": {
      "anyOf": [
        {
          "$ref": "#/$defs/ResolveCachePricing"
        },
        {
          "type": "null"
        }
      ],
      "default": null,
      "description": "Price card for the resolved entry, in USD per 1M tokens."
    },
    "supports_prompt_caching": {
      "anyOf": [
        {
          "type": "boolean"
        },
        {
          "type": "null"
        }
      ],
      "default": null,
      "description": "True only for explicit cache_control breakpoints. Automatic prefix caching needs no client markers.",
      "title": "Supports Prompt Caching"
    },
    "automatic_prefix_caching": {
      "anyOf": [
        {
          "type": "boolean"
        },
        {
          "type": "null"
        }
      ],
      "default": null,
      "description": "Server-side transparent prefix caching.",
      "title": "Automatic Prefix Caching"
    },
    "supports_automatic_prefix_caching": {
      "anyOf": [
        {
          "type": "boolean"
        },
        {
          "type": "null"
        }
      ],
      "default": null,
      "deprecated": true,
      "description": "Per-entry form of automatic_prefix_caching, emitted on the per-provider route.",
      "title": "Supports Automatic Prefix Caching"
    },
    "skipped_entries": {
      "anyOf": [
        {
          "items": {
            "$ref": "#/$defs/SkippedEntry"
          },
          "type": "array"
        },
        {
          "type": "null"
        }
      ],
      "default": null,
      "description": "Registry entries skipped during resolution, with the reason. R12/R29: a consumer that discards this cannot tell a fallback from a primary, and a resolution that dropped the group's declared primary logs identically to one that did not. Reasons distinguish 'not reachable from execution context X', 'no <wire>-wire endpoint declared', 'egress proxy not reachable' and the deprecated exclude_route constraint.",
      "title": "Skipped Entries"
    },
    "exec_context": {
      "anyOf": [
        {
          "enum": [
            "laptop",
            "ec2",
            "lambda",
            "ci"
          ],
          "type": "string"
        },
        {
          "type": "null"
        }
      ],
      "default": null,
      "description": "The execution context this resolution was performed for (model-router-policy R29). DECLARED by the caller, never inferred by the resolver. Entries the registry does not mark reachable_from this context are filtered out and recorded in skipped_entries. Added at schema_version 2 as an additive field; consumers predating it ignore it safely. The enum is rendered from krepis.router.EXEC_CONTEXTS, so a context added there cannot be missing here.",
      "title": "Exec Context"
    },
    "wire": {
      "anyOf": [
        {
          "enum": [
            "anthropic",
            "openai"
          ],
          "type": "string"
        },
        {
          "type": "null"
        }
      ],
      "default": null,
      "description": "The wire format api_base_url speaks. 'anthropic' is the Claude CLI's Messages format (the default); 'openai' is the OpenAI-compatible format programmatic callers on the openai transport use. The same provider is often reachable at both on different ports, so a consumer MUST NOT assume api_base_url matches its own transport without checking this field. Added at schema_version 2 as an additive field.",
      "title": "Wire"
    }
  },
  "required": [
    "schema_version",
    "model",
    "display_name",
    "provider",
    "route",
    "api_base_url",
    "deployment_id",
    "auth_token_type",
    "group",
    "registry_id",
    "capabilities",
    "params"
  ],
  "title": "krepis.router resolve contract",
  "type": "object"
}
